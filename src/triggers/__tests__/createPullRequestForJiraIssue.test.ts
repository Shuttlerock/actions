describe('createPullRequestForJiraIssue', () => {
  xit('sends an error message if no user is assigned to the issue', async () => {
    // Test something
  })

  xit("doesn't make a pull request if the issue has sub-tasks", async () => {
    // Test something
  })

  xit('sends an error message if no repository is set for the issue', async () => {
    // Test something
  })

  xit('returns an existing PR if there is one', async () => {
    // Test something
  })

  xit('creates a new PR', async () => {
    // Test something
  })
})
