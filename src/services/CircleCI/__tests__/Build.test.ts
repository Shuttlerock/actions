import * as Build from '@sr-services/CircleCI/Build'
import { client } from '@sr-services/CircleCI/Client'

const repo = 'my-repo'
const branch = 'my-branch'

describe('Build', () => {
  describe('triggerBuildFor', () => {
    it('calls the CircleCI API', async () => {
      
    })
  })
})
