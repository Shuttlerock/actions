describe('Log', () => {
  describe('debug', () => {
    xit('passes the message on to the actions debugger', async () => {
      // Test something
    })

    xit('handles objects', async () => {
      // Test something
    })
  })
})
