describe('Slack', () => {
  describe('sendErrorMessage', () => {
    xit('posts a message to the Slack error channel', async () => {
      // Test something
    })
  })

  describe('sendUserMessage', () => {
    xit('posts a message to the Slack user', async () => {
      // Test something
    })
  })
})
