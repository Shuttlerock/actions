describe('Credentials', () => {
  describe('getCredentialsByEmail', () => {
    xit('calls the credential API', async () => {
      // Test something
    })

    xit('throws an error if the API returns an error', async () => {
      // Test something
    })
  })
})
