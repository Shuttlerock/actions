describe('Issue', () => {
  describe('getIssue', () => {
    xit('calls the Jira API', async () => {
      // Test something
    })

    xit('includes the repository explicitly', async () => {
      // Test something
    })
  })

  describe('getIssuePullRequestNumbers', () => {
    xit('calls the Jira API', async () => {
      // Test something
    })
  })
})
